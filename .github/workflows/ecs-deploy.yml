name: ECS CI/CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Configure AWS
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Login Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and Push Image
      run: |
        IMAGE_TAG=${{ github.sha }}
        docker build -t ${{ secrets.DOCKER_USERNAME }}/my-nginx:$IMAGE_TAG .
        docker push ${{ secrets.DOCKER_USERNAME }}/my-nginx:$IMAGE_TAG

    - name: Get Current Task Definition
      run: |
        aws ecs describe-task-definition \
          --task-definition tf-nginx-task \
          --query taskDefinition > task-def.json

    - name: Update Task Definition Image
      run: |
        IMAGE_TAG=${{ github.sha }}
        jq --arg IMAGE "${{ secrets.DOCKER_USERNAME }}/my-nginx:$IMAGE_TAG" \
        '.containerDefinitions[0].image = $IMAGE' task-def.json > new-task-def.json

    - name: Register New Task Definition
      run: |
        aws ecs register-task-definition \
          --cli-input-json file://new-task-def.json

    - name: Update ECS Service
      run: |
        aws ecs update-service \
          --cluster tf-ecs-cluster \
          --service tf-ecs-service \
          --force-new-deployment
